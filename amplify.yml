version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
            - ls -R .output
            - mkdir -p .output/compute/default
            - cp .output/server/index.mjs .output/compute/default/server.js
            - ls -R .output/compute/default
      artifacts:
        baseDirectory: .output
        files:
          - '**/*'
      cache:
        paths:
          - .nuxt/cache/**/*
          - node_modules/**/*
    appRoot: ingroup-amplify